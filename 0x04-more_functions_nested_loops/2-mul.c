#include "main.h"
/**
 * mul - mulltiplies two integers
 * @a: to be multiplied to b
 * @b: to be multiplied to a
 *
 * Return: multiplied value (a * b)
 */
int mul(int a, int b)
{
	return (a * b);
}
